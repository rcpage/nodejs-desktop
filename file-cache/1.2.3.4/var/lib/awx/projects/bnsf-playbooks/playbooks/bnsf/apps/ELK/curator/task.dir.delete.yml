---
- set_fact:
    paths_to_remove:
          - "{{ curator.logsDirectory }}"
          - "{{ curator.directory }}"

- name: "Remove directories from file system"          
  #become: yes
  file:
   path: "{{ item }}"   
   state: absent
  with_items: "{{ paths_to_remove }}"
