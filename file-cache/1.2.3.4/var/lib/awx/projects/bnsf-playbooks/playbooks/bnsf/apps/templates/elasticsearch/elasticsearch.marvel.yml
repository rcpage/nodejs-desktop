# ======================== Elasticsearch Configuration =========================
#
# NOTE: Elasticsearch comes with reasonable defaults for most settings.
#       Before you set out to tweak and tune the configuration, make sure you
#       understand what are you trying to accomplish and the consequences.
#
# The primary way of configuring a node is via this file. This template lists
# the most important settings you may want to configure for a production cluster.
#
# Please see the documentation for further information on configuration options:
# <http://www.elastic.co/guide/en/elasticsearch/reference/current/setup-configuration.html>
#
# ---------------------------------- Cluster -----------------------------------
#
# Use a descriptive name for your cluster:
#
cluster.name: {{elasticSearch.clusterName}}
#
# ------------------------------------ Node ------------------------------------
#
# Use a descriptive name for the node:
#
node.name: {{elasticSearch.node.name}}
#
# Add custom attributes to the node:
#
# node.rack: r1
#
# Master node setting
#
node.master: {{elasticSearch.node.master}}
node.data: {{elasticSearch.node.data}}
node.ingest: {{elasticSearch.node.ingest}}
#
# ----------------------------------- Paths ------------------------------------
#
# Path to directory where to store the data (separate multiple locations by comma):
#
path.data: {{elasticSearch.dataDirectory}}
#
# Path to log files:
#
path.logs: {{elasticSearch.logsDirectory}}
#
# ----------------------------------- Memory -----------------------------------
# Make sure that the `ES_HEAP_SIZE` environment variable is set to about half the memory
# available on the system and that the owner of the process is allowed to use this limit.
#
# Elasticsearch performs poorly when the system is swapping the memory.
#
# ---------------------------------- Network -----------------------------------
#
# Set the bind address to a specific IP (IPv4 or IPv6):
#
network.host: {{ip_address}}
network.bind_host: {{ip_address}}
network.publish_host: {{ip_address}}
#
# Set a custom port for HTTP:
#
http.port: {{elasticSearch.port}}
#
# For more information, see the documentation at:
# <http://www.elastic.co/guide/en/elasticsearch/reference/current/modules-network.html>
#
# --------------------------------- Discovery ----------------------------------
#
# Pass an initial list of hosts to perform discovery when new node is started:
# The default list of hosts is ["127.0.0.1", "[::1]"]
#
discovery.zen.ping.unicast.hosts: [{{elasticSearch.marvelZenHosts}}]
#
# For more information, see the documentation at:
# <http://www.elastic.co/guide/en/elasticsearch/reference/current/modules-discovery.html>
#
# ---------------------------------- Gateway -----------------------------------
#
# Block initial recovery after a full cluster restart until N nodes are started:
#
gateway.recover_after_nodes: {{ elasticSearch.gateway.recover_after_nodes }}
#
# For more information, see the documentation at:
# <http://www.elastic.co/guide/en/elasticsearch/reference/current/modules-gateway.html>
#
# ---------------------------------- Various -----------------------------------
#
# Disable starting multiple nodes on a single system:
#
# node.max_local_storage_nodes: 1
#
# Require explicit names when deleting indices:
#
# action.destructive_requires_name: true
http.max_content_length: {{ elasticSearch['http.max_content_length'] }}
transport.tcp.port: {{elasticSearch['transport.tcp.port']}}
transport.connections_per_node.recovery: {{elasticSearch['transport.connections_per_node.recovery']}}
transport.connections_per_node.bulk: {{elasticSearch['transport.connections_per_node.bulk']}}
transport.connections_per_node.reg: {{elasticSearch['transport.connections_per_node.reg']}}
transport.connections_per_node.state: {{elasticSearch['transport.connections_per_node.state']}}
transport.connections_per_node.ping: {{elasticSearch['transport.connections_per_node.ping']}}

xpack.monitoring.enabled: true
xpack.monitoring.exporters.type1.type: http
xpack.monitoring.exporters.type1.host: [{{elasticSearch.marvelAgent}}]
xpack.monitoring.exporters.type1.auth.username: {{elasticSearch.user.elastic}}
xpack.monitoring.exporters.type1.auth.password: {{elastic_password}}

xpack.security.authc.realms.active_directory.type: active_directory
xpack.security.authc.realms.active_directory.order: 1
xpack.security.authc.realms.active_directory.domain_name: rails.rwy.bnsf.com
xpack.security.authc.realms.active_directory.url: ldaps://rails.rwy.bnsf.com:636
xpack.security.authc.realms.active_directory.unmapped_groups_as_roles: false
xpack.security.authc.realms.active_directory.files.role_mapping: "{{xPackClientPlugin.configDirectory}}/role_mapping.yml"
xpack.security.authc.realms.active_directory.ssl.certificate_authorities: {{elasticSearch.bnsfCACert}}
xpack.security.authc.realms.file1.type: file
xpack.security.authc.realms.file1.order: 2
xpack.security.authc.realms.native1.type: native
xpack.security.authc.realms.native1.order: 0
